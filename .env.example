# Copy to .env and fill in real values. .env is gitignored — never commit real credentials.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require
# Keyed-hash secret for anti-enumeration dummy KDF salts (>=32 random chars)
SERVER_SECRET=GENERATE_A_RANDOM_64_CHAR_HEX_STRING
